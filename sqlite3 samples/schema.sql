CREATE TABLE `team`
(
 `id`         INTEGER PRIMARY KEY AUTOINCREMENT,
 `team_name`  NOT NULL ,
 `acronym`    NOT NULL ,
 `city`       NOT NULL
);

-- .mode column
-- .headers on
-- .schema
-- .tables
-- .read nomdufichier.sql
